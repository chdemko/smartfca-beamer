# Beamer template for SmartFCA

Usage:

~~~latex
\usetheme{SmartFCA}
~~~

or use this option:

~~~latex
\usetheme[pagenumber]{SmartFCA}
~~~

The output of `template.tex` gives the `template.pdf` file.

